/**
 * Compatibility re-export barrel — feature slices import models from here per
 * docs/CODING_STANDARDS.md folder layout. The real model classes + associations
 * live in src/database/models/index.ts.
 */
export * from '../../../database/models';
